[build-system]
requires = ["setuptools>=45"]
build-backend = "setuptools.build_meta"

[project]
name = "tinydrama"
version = "0.1.0"
description = "轻量级浏览器自动化库，直连 Chrome CDP"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]
